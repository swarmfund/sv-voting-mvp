language: node_js
node_js:
    - "8"
before_install: 
    - echo "hi"
install:
    - yarn install
    - |
      testrpc &
      TESTRPC_PID=$!
script:
    "yarn test-all"
after_script:
    - kill -s SIGTERM $TESTRPC_PID